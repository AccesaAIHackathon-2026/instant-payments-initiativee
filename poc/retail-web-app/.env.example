# Copy this file to .env and fill in the values.
# .env is gitignored — never commit real keys.

# API key sent to the bank simulator on every request (X-Api-Key header / ?apiKey= for SSE)
VITE_BANK_API_KEY=blinkpay-poc-key

